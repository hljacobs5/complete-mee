import fs from 'fs';

const Node = require('../lib/Node');

class Trie {
  constructor(data) {
    this.node = node;
    this.root = new Node;
    this.suggestWord = [];
    this.count = 0;
  }

  insert(data) {
    if (!this.node) {
      this.node = new Node(data);
    }

    let currentNode = 

  }

  find(data) {
    if (!node.data) {
      
    }
  }
}

module.export = Trie;